# Database Configuration
MONGODB=mongodb://localhost:27017/live-solution
MONGODB_PROD=mongodb://localhost:27017/live-solution-prod

# JWT Configuration
# JWT_SECRET: Used for NestJS-generated tokens (login, signup, etc.)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# SSO JWT Configuration
# ⚠️ CRITICAL: JWT_SECRET_KEY must be EXACTLY the same as PHP website's secret!
# This is used to verify JWT tokens generated by the PHP website
# Must be at least 32 characters long for security
JWT_SECRET_KEY=your-php-website-jwt-secret-key-must-match-exactly-min-32-chars

# Server Configuration
PORT=3007
NODE_ENV=development

# LiveKit Configuration
# LiveKit is running on server .91
LIVEKIT_URL=http://39.116.130.91:7880
# Default development keys (replace with actual production keys from .91 server)
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=secret

# CORS Configuration for Cross-Domain SSO
# ⚠️ IMPORTANT: These domains must match your actual deployment URLs
PHP_WEBSITE_URL=https://livekit1.hrdeedu.com
NESTJS_FRONTEND_URL=https://live.hrdeedu.co.kr
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# File Upload Configuration
MAX_FILE_SIZE=50000000
UPLOAD_PATH=./uploads

# VOD Server Configuration (Recording Storage)
# Recording path: /root/Desktop/Live-Solution/Recordings

# Disable mount (not needed in this setup)
VOD_SERVER_ENABLED=false

# Recording path on .91 server (where LiveKit is running)
VOD_SERVER_FALLBACK_PATH=/root/Desktop/Live-Solution/Recordings

# VOD Server Details (for reference)
VOD_SERVER_HOST=39.116.130.91
VOD_SERVER_PORT=2222
VOD_SERVER_USER=root
VOD_SERVER_REMOTE_PATH=/root/Desktop/Live-Solution/Recordings

# Public URL to access recordings (served from .91)
VOD_SERVER_PUBLIC_URL=http://39.116.130.91/recordings

